CREATE TABLE Article (
     title TEXT NOT NULL,
     desc TEXT,
     date TEXT NOT NULL,
     imgUrl TEXT
);

insertArticle:
INSERT INTO Article(title, desc, date, imgUrl)
VALUES(?,?, ?, ?);

selectAllArticles:
SELECT Article.* FROM  Article;

removAllArticles:
DELETE FROM Article;
